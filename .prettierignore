build
.svelte-kit
node_modules
pnpm-lock.yaml
Dockerfile
.prettierignore
.gitignore
.dockerignore

# Prettier 3 has no SQL parser and no plugin is installed. Migrations are
# hand-authored and reviewed via diff — letting the format hook see them
# fails the commit before lint/typecheck even run.
*.sql
